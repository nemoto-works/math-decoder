function alignFourDigitDivision() {
  // Deprecated: direct DOM coordinate rewrites caused highlight drift.
  // Keep this file as a no-op so cached index.html references stay safe.
}

window.addEventListener('load', alignFourDigitDivision);
